apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - rbac.yaml
  - cert-issuer.yaml
  - network-policy.yaml
  - secrets/              # ← SECRETS FÖRST
  - storage/             # ← STORAGE ANDRA
  - helmreleases/        # ← HELMRELEASES TREDJE
  - prometheus/          # ← HELM CHART
  - servicemonitor.yaml
  - auth-middleware/
  - ingress/
