apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.middlewares: "monitoring-basic-auth@kubernetescrd,monitoring-ratelimit@kubernetescrd"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - grafana.example.com  # ← ÄNDRA DIN DOMÄN
      secretName: grafana-tls
  rules:
    - host: grafana.example.com  # ← ÄNDRA DIN DOMÄN
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-prometheus-stack-grafana
                port:
                  number: 80
