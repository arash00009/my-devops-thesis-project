apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  environment: staging
resources:
  - namespace.yaml
  - nginx-app.yaml
  - nginx-network-policy.yaml
  # nginx-ingress.yaml
  - infrastructure-traefik.yaml
  - monitoring/      # Detta räcker, Flux hittar allt i mappen
