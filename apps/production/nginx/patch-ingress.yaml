apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx
  namespace: nginx
spec:
  tls:
    - hosts:
        - production.cloudist.solutions
      secretName: nginx-tls-secret
  rules:
    - host: production.cloudist.solutions
      http:
        paths:
          - path: /nginx
            pathType: Prefix
            backend:
              service:
                name: nginx
                port:
                  number: 80