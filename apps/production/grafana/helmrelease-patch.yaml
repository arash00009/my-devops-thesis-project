apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana
  namespace: grafana
spec:
  values:
    grafana.ini:
      server:
        root_url: https://lia-prod.cloudist.solutions/grafana