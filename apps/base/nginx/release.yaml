apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nginx
  namespace: nginx
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: bitnami-nginx
    namespace: flux-system
  values:
    replicaCount: 2
    service:
      type: ClusterIP
      port: 80